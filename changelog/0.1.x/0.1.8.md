---
summary: "Run the release:github script under bun instead of tsx, matching the rest of the script toolchain"
breaking: false
security: false
---

# 0.1.8 — 2026-06-15

## Fixed

- **`release:github` script** now runs under `bun` (`bun run scripts/release-github.ts`) instead of `tsx`, which is not a declared dependency — aligning it with every other `scripts/*.ts` entry in `package.json`.

## Dependencies

- `vitest` `^4.1.8 → ^4.1.9`
